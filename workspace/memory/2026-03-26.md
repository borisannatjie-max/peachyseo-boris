# Diary — Thursday 26 March 2026

## Morning / Midday Sessions

- **PDF to MD/DOCX conversion project kicked off.** David has a chemistry textbook PDF he wants converted to markdown. Since it's very long (~1359 pages across 14 batches), Boris proposed a batched approach: split into 14 batches of ~100 pages each, convert each to markdown, then stitch together. Created `/tmp/ocr_batch.py` for this.
- **Image analysis problem.** David noticed Boris couldn't see images sent via Telegram or webchat. MiniMax-M2.7 apparently doesn't support image inputs natively. David provided an OpenAI API key (`sk-proj-...`) to use as a fallback for image analysis. Boris was in the process of adding it to the OpenClaw config when things got a bit tangled — David restarted the gateway mid-process.
- **Gmail/Google Drive setup attempt.** David created a dedicated Gmail account `peachyseo123@gmail.com` and shared a Google Drive folder (`https://drive.google.com/drive/folders/1SAuYSH-P9L_f06zbHjS7KFxNo5zhlE8l`) for Boris to use for file storage. Several attempts were made to set up OAuth 2.0 authentication but they ran into `invalid_scope` errors — the scopes had malformed URLs (`https:/` instead of `https://`). Authentication was not fully completed by end of day.

## Afternoon / Evening

- **OCR batch job running.** Started around 23:46. The Chemistry of the Elements PDF is being processed batch-by-batch. Output is a DOCX file at `/root/.openclaw/workspace/Chemistry_of_Elements.docx` (56KB so far). Progress: Batch 1/14 — 25/100 pages done as of 23:52. Job still running at midnight.
- **Image test at end of day.** David sent a photo of a Kikkoman soy sauce bottle. Boris described it but David said "Nope" — not sure if the description was wrong or he was just testing.

## Pending / Follow-ups

1. **Gmail OAuth** — Needs to be re-attempted with correct scope formatting (`https://www.googleapis.com/auth/gmail.send` etc., not `https:/`)
2. **PDF conversion** — Still running (14 batches total). OCR check cron job running every 5 mins. DOCX will be at `/root/.openclaw/workspace/Chemistry_of_Elements.docx` when done.
3. **OpenAI image fallback** — Was being configured when gateway was restarted; unclear if it was successfully saved to openclaw.json
4. **Email to hello@peachyseo.com** — Still broken (554 relay access denied). May need MX record review.
5. **Image analysis** — Still untested/unconfirmed working after the OpenAI fallback setup attempt
